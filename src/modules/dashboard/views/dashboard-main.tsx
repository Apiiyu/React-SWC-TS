// Components
import { Hero } from '../components/Hero';

/**
 * @description Composes the dashboard view from its feature-local hero component.
 */
export const Dashboard = () => {
  return <Hero />;
};
